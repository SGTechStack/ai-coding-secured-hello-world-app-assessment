# Integration tests: the schema plus the demo account. Cookie flags stay at their secure defaults.
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/seed

app:
  cors:
    allowed-origins: http://localhost:3000
  frontend-url: http://localhost:3000
  admin:
    # The dev bootstrap admin (see application-dev.yml); SpaAuthFlow.logInAsAdmin uses it.
    username: admin
    email: admin@example.com
    password: Dev-Admin-Passw0rd!
