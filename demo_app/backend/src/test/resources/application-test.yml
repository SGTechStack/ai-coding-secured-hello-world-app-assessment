# Integration tests: the schema plus the demo account. Cookie flags stay at their secure defaults.
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/seed

app:
  cors:
    allowed-origins: http://localhost:3000
